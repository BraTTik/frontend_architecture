import React from "react";
import { PlayerPage } from "./pages";
import {AppContainer} from "app/components";

export const App = () => {
    return <AppContainer>
        <PlayerPage />
    </AppContainer>;
}
